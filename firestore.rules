rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Default: allow read/write for 30 days while developing.
      // Replace with proper auth rules before production.
      allow read, write: if request.time < timestamp.date(2026, 3, 5);
    }
  }
}
