# Cursor / local debug logs (folder kept via .gitkeep)
.cursor/debug-logs/*.log

# macOS
.DS_Store

# Xcode user state (per-user, should never be committed)
*.xcuserstate

# Node.js dependencies (e.g. if using Firebase functions)
functions/node_modules/
