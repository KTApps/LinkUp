rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Default: deny all. Tighten with auth when ready.
      allow read, write: if false;
    }
  }
}
